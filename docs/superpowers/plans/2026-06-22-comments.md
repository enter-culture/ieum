# 댓글(Comments) DB 연동 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 더미 댓글을 실제 DB 연동(조회+작성)으로 교체한다.

**Architecture:** 백엔드 `ieum-api`에 `likes` 모듈을 미러링한 `comments` 모듈(NestJS + MikroORM)을 추가하고(조회 공개 / 작성 `JwtAuthGuard`), 프론트 `ieum`의 `CommentDrawer`를 SWR 조회 + 낙관적 작성으로 연결한다.

**Tech Stack:** NestJS, MikroORM(`@mikro-orm/postgresql` v7, `defineEntity`), class-validator, Next.js, SWR, fetch.

## Global Constraints

- 백엔드 리포: `/Users/sinseongsu/Desktop/code/ieum-api` · 프론트 리포: `/Users/sinseongsu/Desktop/code/ieum` (별도 리포, 각각 커밋).
- 영상 식별자는 정수 `shortsId`이며 프론트 `ShortsPlace.id`와 동일(=`likes`와 동일 규약).
- 작성 인증: 백엔드 `JwtAuthGuard`, 프론트 `requireAuth`. 조회는 인증 없음.
- 작성자 표시 이름은 `nickname ?? name`, 아바타는 `picture`(없으면 이니셜).
- 본문: 비어있지 않음, 최대 500자.
- 정렬: `createdAt DESC`. 페이지네이션 없음(MVP).
- 스키마 반영은 기존 워크플로우 `npm run schema:update` 사용(별도 마이그레이션 파일 작성 안 함).
- MikroORM은 `defineEntity`/`p` API 사용(데코레이터 클래스 아님). 엔티티 파일은 `likes/like.entity.ts` 형식을 따른다.

---

## 백엔드 (ieum-api)

### Task 1: Comment 엔티티 + 모듈 등록 + 스키마 반영

**Files:**
- Create: `ieum-api/src/comments/comment.entity.ts`
- Create: `ieum-api/src/comments/comments.module.ts`
- Modify: `ieum-api/src/app.module.ts` (import + imports 배열에 `CommentsModule` 추가)

**Interfaces:**
- Produces: `Comment`(엔티티), `IComment`(타입), `CommentsModule`.

- [ ] **Step 1: 엔티티 작성** — `ieum-api/src/comments/comment.entity.ts`

```ts
import type { InferEntity } from '@mikro-orm/core';
import { defineEntity, p } from '@mikro-orm/postgresql';
import { User } from '../users/user.entity';

export const Comment = defineEntity({
  name: 'Comment',
  tableName: 'comments',
  properties: {
    id: p.integer().primary(),
    shortsId: p.integer(),
    user: () => p.manyToOne(User),
    content: p.text(),
    createdAt: p.datetime().onCreate(() => new Date()),
  },
  indexes: [{ properties: ['shortsId', 'createdAt'] }],
});

export type IComment = InferEntity<typeof Comment>;
```

- [ ] **Step 2: 모듈 작성** — `ieum-api/src/comments/comments.module.ts` (CommentsController/Service는 Task 2~3에서 생성하나, 모듈 파일은 그들을 참조하므로 이 시점에 함께 작성한다. import 대상 파일이 아직 없으면 Task 2 완료 후 빌드한다.)

```ts
import { MikroOrmModule } from '@mikro-orm/nestjs';
import { Module } from '@nestjs/common';
import { AuthModule } from '../auth/auth.module';
import { User } from '../users/user.entity';
import { Comment } from './comment.entity';
import { CommentsController } from './comments.controller';
import { CommentsService } from './comments.service';

@Module({
  imports: [MikroOrmModule.forFeature([Comment, User]), AuthModule],
  controllers: [CommentsController],
  providers: [CommentsService],
})
export class CommentsModule {}
```

- [ ] **Step 3: app.module.ts 등록**

`import { CommentsModule } from './comments/comments.module';`를 import 블록에 추가하고, `imports: [...]` 배열에서 `LikesModule` 다음 줄에 `CommentsModule,`를 추가한다.

- [ ] **Step 4: 스키마 반영** (Task 2·3의 service/controller 작성 후 빌드가 되는 시점에 실행)

Run: `cd /Users/sinseongsu/Desktop/code/ieum-api && npm run schema:update`
Expected: `comments` 테이블 + `(shorts_id, created_at)` 인덱스 생성 SQL 실행 성공.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sinseongsu/Desktop/code/ieum-api
git add src/comments/comment.entity.ts src/comments/comments.module.ts src/app.module.ts
git commit -m "feat(comments): Comment 엔티티 + 모듈 스캐폴딩"
```

---

### Task 2: CommentsService (조회/작성) + 단위 테스트

**Files:**
- Create: `ieum-api/src/comments/comments.service.ts`
- Create: `ieum-api/src/comments/comments.service.spec.ts`

**Interfaces:**
- Consumes: `Comment`(Task 1), `User` 엔티티, MikroORM `EntityManager`.
- Produces:
  - `interface CommentDto { id: number; shortsId: number; content: string; createdAt: Date; author: { id: number; nickname: string | null; name: string; picture: string | null } }`
  - `CommentsService.list(shortsId: number): Promise<CommentDto[]>`
  - `CommentsService.create(userId: number, dto: { shortsId: number; content: string }): Promise<CommentDto>`

- [ ] **Step 1: 실패하는 테스트 작성** — `ieum-api/src/comments/comments.service.spec.ts`

기존 `*.service.spec.ts` 패턴(있으면)을 따른다. EntityManager는 `find`/`create`/`flush`를 가진 목으로 주입한다.

```ts
import { CommentsService } from './comments.service';

describe('CommentsService', () => {
  const user = { id: 7, nickname: '구수', name: '신성수', picture: 'http://img/p.png' };

  function makeEm(overrides: Partial<Record<string, jest.Mock>> = {}) {
    return {
      find: jest.fn(),
      findOne: jest.fn(),
      create: jest.fn(),
      flush: jest.fn(),
      getReference: jest.fn((_e: unknown, id: number) => ({ id })),
      ...overrides,
    } as never;
  }

  it('list: shortsId의 댓글을 최신순으로 DTO 매핑한다', async () => {
    const em = makeEm({
      find: jest.fn().mockResolvedValue([
        { id: 2, shortsId: 10, content: '두번째', createdAt: new Date('2026-06-22'), user },
        { id: 1, shortsId: 10, content: '첫번째', createdAt: new Date('2026-06-21'), user },
      ]),
    });
    const svc = new CommentsService(em);
    const res = await svc.list(10);
    expect(em.find).toHaveBeenCalledWith(
      expect.anything(),
      { shortsId: 10 },
      { orderBy: { createdAt: 'DESC' }, populate: ['user'] },
    );
    expect(res[0]).toEqual({
      id: 2, shortsId: 10, content: '두번째', createdAt: new Date('2026-06-22'),
      author: { id: 7, nickname: '구수', name: '신성수', picture: 'http://img/p.png' },
    });
  });

  it('create: 댓글을 만들고 작성자 정보를 포함해 반환한다', async () => {
    const created = { id: 5, shortsId: 10, content: '새 댓글', createdAt: new Date('2026-06-22'), user };
    const em = makeEm({ create: jest.fn().mockReturnValue(created) });
    const svc = new CommentsService(em);
    const res = await svc.create(7, { shortsId: 10, content: '새 댓글' });
    expect(em.flush).toHaveBeenCalled();
    expect(res).toEqual({
      id: 5, shortsId: 10, content: '새 댓글', createdAt: new Date('2026-06-22'),
      author: { id: 7, nickname: '구수', name: '신성수', picture: 'http://img/p.png' },
    });
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd /Users/sinseongsu/Desktop/code/ieum-api && npx jest src/comments/comments.service.spec.ts`
Expected: FAIL — `Cannot find module './comments.service'`.

- [ ] **Step 3: 서비스 구현** — `ieum-api/src/comments/comments.service.ts`

```ts
import { EntityManager } from '@mikro-orm/core';
import { Injectable } from '@nestjs/common';
import { User } from '../users/user.entity';
import { Comment, IComment } from './comment.entity';

export interface CommentDto {
  id: number;
  shortsId: number;
  content: string;
  createdAt: Date;
  author: {
    id: number;
    nickname: string | null;
    name: string;
    picture: string | null;
  };
}

@Injectable()
export class CommentsService {
  constructor(private readonly em: EntityManager) {}

  async list(shortsId: number): Promise<CommentDto[]> {
    const comments = await this.em.find(
      Comment,
      { shortsId },
      { orderBy: { createdAt: 'DESC' }, populate: ['user'] },
    );
    return comments.map((c) => this.toDto(c));
  }

  async create(
    userId: number,
    dto: { shortsId: number; content: string },
  ): Promise<CommentDto> {
    const comment = this.em.create(Comment, {
      shortsId: dto.shortsId,
      content: dto.content,
      user: this.em.getReference(User, userId),
    });
    await this.em.flush();
    return this.toDto(comment as unknown as IComment & { user: User });
  }

  private toDto(c: IComment & { user: User }): CommentDto {
    return {
      id: c.id,
      shortsId: c.shortsId,
      content: c.content,
      createdAt: c.createdAt,
      author: {
        id: c.user.id,
        nickname: c.user.nickname ?? null,
        name: c.user.name,
        picture: c.user.picture ?? null,
      },
    };
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd /Users/sinseongsu/Desktop/code/ieum-api && npx jest src/comments/comments.service.spec.ts`
Expected: PASS (2 passing).

- [ ] **Step 5: 커밋**

```bash
cd /Users/sinseongsu/Desktop/code/ieum-api
git add src/comments/comments.service.ts src/comments/comments.service.spec.ts
git commit -m "feat(comments): 조회/작성 서비스 + 단위 테스트"
```

---

### Task 3: CommentsController + DTO + 인증/공개 동작 검증

**Files:**
- Create: `ieum-api/src/comments/dto/create-comment.dto.ts`
- Create: `ieum-api/src/comments/comments.controller.ts`

**Interfaces:**
- Consumes: `CommentsService`(Task 2), `JwtAuthGuard`, `AuthUser`(`req.user.uid`).
- Produces: `GET /comments?shortsId=` (공개), `POST /comments` (인증). `CreateCommentDto { shortsId: number; content: string }`.

- [ ] **Step 1: DTO 작성** — `ieum-api/src/comments/dto/create-comment.dto.ts`

```ts
import { ApiProperty } from '@nestjs/swagger';
import { IsInt, IsNotEmpty, IsString, MaxLength } from 'class-validator';

export class CreateCommentDto {
  @ApiProperty({ description: '프론트 ShortsPlace.id' })
  @IsInt()
  shortsId: number;

  @ApiProperty({ description: '댓글 본문(최대 500자)' })
  @IsString()
  @IsNotEmpty()
  @MaxLength(500)
  content: string;
}
```

- [ ] **Step 2: 컨트롤러 작성** — `ieum-api/src/comments/comments.controller.ts`

`likes.controller.ts`와 달리 가드를 컨트롤러 레벨이 아니라 POST 메서드에만 건다.

```ts
import {
  Body,
  Controller,
  Get,
  ParseIntPipe,
  Post,
  Query,
  Req,
  UseGuards,
} from '@nestjs/common';
import { ApiBearerAuth, ApiOperation, ApiTags } from '@nestjs/swagger';
import type { Request } from 'express';
import type { AuthUser } from '../auth/auth.service';
import { JwtAuthGuard } from '../auth/jwt-auth.guard';
import { CommentsService } from './comments.service';
import { CreateCommentDto } from './dto/create-comment.dto';

@ApiTags('comments')
@Controller('comments')
export class CommentsController {
  constructor(private readonly commentsService: CommentsService) {}

  @Get()
  @ApiOperation({ summary: '영상 댓글 목록(공개)' })
  list(@Query('shortsId', ParseIntPipe) shortsId: number) {
    return this.commentsService.list(shortsId);
  }

  @Post()
  @ApiBearerAuth()
  @UseGuards(JwtAuthGuard)
  @ApiOperation({ summary: '댓글 작성(로그인 필요)' })
  create(
    @Req() req: Request & { user: AuthUser },
    @Body() dto: CreateCommentDto,
  ) {
    return this.commentsService.create(req.user.uid, dto);
  }
}
```

> `req.user.uid`는 `likes.controller.ts`와 동일한 접근 방식이다. 만약 `AuthUser`의 식별 필드명이 다르면 `likes.controller.ts`가 쓰는 필드명을 그대로 따른다.

- [ ] **Step 3: 빌드 + 스키마 반영(Task 1 Step 4 미실행 시 지금)**

Run: `cd /Users/sinseongsu/Desktop/code/ieum-api && npm run build && npm run schema:update`
Expected: 빌드 성공, `comments` 테이블 생성.

- [ ] **Step 4: 서버 기동 후 동작 검증**

Run(서버 기동): `cd /Users/sinseongsu/Desktop/code/ieum-api && npm run start:dev` (별도 터미널)

검증:
```bash
# 공개 조회 — 200 + 빈 배열
curl -s "http://localhost:4000/api/comments?shortsId=1"
# 미인증 작성 — 401
curl -s -o /dev/null -w "%{http_code}\n" -X POST "http://localhost:4000/api/comments" \
  -H "Content-Type: application/json" -d '{"shortsId":1,"content":"hi"}'
```
Expected: 첫 줄 `[]`, 둘째 `401`.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sinseongsu/Desktop/code/ieum-api
git add src/comments/comments.controller.ts src/comments/dto/create-comment.dto.ts
git commit -m "feat(comments): 컨트롤러(GET 공개/POST 인증) + DTO"
```

---

## 프론트엔드 (ieum)

### Task 4: 댓글 API 클라이언트

**Files:**
- Create: `ieum/src/shared/api/comments.ts`

**Interfaces:**
- Consumes: `apiUrl`(`@/shared/api/base`), `getToken`(`@/shared/lib/auth-token`).
- Produces:
  - `interface CommentAuthor { id: number; nickname: string | null; name: string; picture: string | null }`
  - `interface CommentItem { id: number; shortsId: number; content: string; createdAt: string; author: CommentAuthor }`
  - `getComments(shortsId: number): Promise<CommentItem[]>`
  - `createComment(shortsId: number, content: string): Promise<CommentItem>`

- [ ] **Step 1: API 클라이언트 작성** — `ieum/src/shared/api/comments.ts`

`likes.ts`의 `authHeaders` 패턴을 그대로 따른다.

```ts
import { apiUrl } from "@/shared/api/base";
import { getToken } from "@/shared/lib/auth-token";

export interface CommentAuthor {
  id: number;
  nickname: string | null;
  name: string;
  picture: string | null;
}

export interface CommentItem {
  id: number;
  shortsId: number;
  content: string;
  createdAt: string;
  author: CommentAuthor;
}

function authHeaders(): Record<string, string> {
  const token = getToken();
  return token ? { Authorization: `Bearer ${token}` } : {};
}

/** 영상 댓글 목록(공개) */
export async function getComments(shortsId: number): Promise<CommentItem[]> {
  const res = await fetch(apiUrl(`/comments?shortsId=${shortsId}`));
  if (!res.ok) throw new Error("댓글을 불러오지 못했어요");
  return res.json();
}

/** 댓글 작성(로그인 필요) */
export async function createComment(
  shortsId: number,
  content: string,
): Promise<CommentItem> {
  const res = await fetch(apiUrl("/comments"), {
    method: "POST",
    headers: { "Content-Type": "application/json", ...authHeaders() },
    body: JSON.stringify({ shortsId, content }),
  });
  if (!res.ok) throw new Error("댓글 작성에 실패했어요");
  return res.json();
}
```

- [ ] **Step 2: 타입체크**

Run: `cd /Users/sinseongsu/Desktop/code/ieum && npx tsc --noEmit src/shared/api/comments.ts 2>&1 | grep comments.ts || echo "no errors in comments.ts"`
Expected: `no errors in comments.ts`.

- [ ] **Step 3: 커밋**

```bash
cd /Users/sinseongsu/Desktop/code/ieum
git add src/shared/api/comments.ts
git commit -m "feat(comments): 프론트 댓글 API 클라이언트"
```

---

### Task 5: CommentDrawer DB 연동 + shortsId 전달

**Files:**
- Modify: `ieum/src/widgets/shorts-swiper/ui/Shorts.tsx` (CommentDrawer에 `shortsId={item.id}` 전달)
- Modify: `ieum/src/widgets/shorts-swiper/ui/CommentDrawer.tsx` (더미 제거 → SWR 조회 + 낙관적 작성)

**Interfaces:**
- Consumes: `getComments`/`createComment`/`CommentItem`(Task 4), `useAuth().requireAuth`(`@/shared/lib/auth-store`), `useSWR`.
- Produces: 없음(말단 UI).

- [ ] **Step 1: Shorts.tsx에서 shortsId 전달**

`Shorts.tsx`의 CommentDrawer 사용부를 다음으로 변경:

```tsx
<CommentDrawer
  open={commentOpen}
  onClose={() => setCommentOpen(false)}
  shortsId={item.id}
  videoTitle={item.title}
/>
```

- [ ] **Step 2: CommentDrawer.tsx 재작성** — 더미 state 제거, SWR 조회 + 낙관적 작성

props 인터페이스에 `shortsId: number` 추가. 본문 핵심 변경:

```tsx
"use client";
import { useRef, useEffect, useState } from "react";
import useSWR from "swr";
import { getComments, createComment, CommentItem } from "@/shared/api/comments";
import { useAuth } from "@/shared/lib/auth-store";

interface CommentDrawerProps {
  open: boolean;
  onClose: () => void;
  shortsId: number;
  videoTitle: string;
}

export default function CommentDrawer({ open, onClose, shortsId }: CommentDrawerProps) {
  const { requireAuth } = useAuth();
  const { data: comments = [], mutate } = useSWR<CommentItem[]>(
    open ? ["comments", shortsId] : null,
    () => getComments(shortsId),
  );
  const [input, setInput] = useState("");
  const [dragY, setDragY] = useState(0);
  const inputRef = useRef<HTMLInputElement>(null);
  const startY = useRef<number | null>(null);
  const isDragging = useRef(false);

  useEffect(() => {
    if (open) setTimeout(() => inputRef.current?.focus(), 350);
  }, [open]);

  const submit = async (text: string) => {
    // 낙관적 추가
    const optimistic: CommentItem = {
      id: -Date.now(),
      shortsId,
      content: text,
      createdAt: new Date().toISOString(),
      author: { id: -1, nickname: "나", name: "나", picture: null },
    };
    await mutate(
      async (prev = []) => {
        const saved = await createComment(shortsId, text);
        return [saved, ...prev];
      },
      {
        optimisticData: [optimistic, ...comments],
        rollbackOnError: true,
        revalidate: false,
      },
    );
  };

  const handleSubmit = () => {
    const text = input.trim();
    if (!text) return;
    requireAuth(() => {
      setInput("");
      void submit(text);
    });
  };

  // ...(드래그/딤/드로어 마크업은 기존 유지)...
  // 헤더: 댓글 {comments.length}개
  // 목록: comments.map((c) => 아바타 + 작성자명 + 본문)
}
```

작성자 표시(목록 렌더 부분):

```tsx
{comments.map((c) => {
  const displayName = c.author.nickname ?? c.author.name;
  return (
    <div key={c.id} className="flex gap-3">
      {c.author.picture ? (
        <img src={c.author.picture} alt={displayName}
          className="w-8 h-8 rounded-full object-cover flex-shrink-0" />
      ) : (
        <div className="w-8 h-8 rounded-full bg-gray-200 flex-shrink-0 flex items-center justify-center text-xs text-gray-500 font-bold">
          {displayName.slice(0, 1)}
        </div>
      )}
      <div className="flex flex-col gap-0.5">
        <span className="text-xs text-gray-500 font-semibold">{displayName}</span>
        <p className="text-sm text-gray-800">{c.content}</p>
      </div>
    </div>
  );
})}
```

> 기존 딤 배경/드로어 transform/드래그 핸들러/입력창 마크업(`onTouchStart` 등)은 그대로 유지하고, `comments` 데이터 소스와 `handleSubmit`만 위 내용으로 교체한다. `createdAt` 상대표기(예: "방금 전")는 MVP 범위 밖이므로 표시하지 않는다(작성자명으로 대체).

- [ ] **Step 3: 타입체크 + 린트**

Run: `cd /Users/sinseongsu/Desktop/code/ieum && npx tsc --noEmit 2>&1 | grep -E "Shorts.tsx|CommentDrawer.tsx" || echo "no errors in changed files"`
Expected: `no errors in changed files` (기존 `__tests__`의 jest 타입 에러는 무관).

- [ ] **Step 4: 로컬 동작 검증**

`ieum-api`(start:dev)와 `ieum`(`npm run dev`)를 모두 기동한 뒤 `/explore`에서:
1. 댓글 버튼 → 드로어 열림 → 빈 목록(또는 기존 댓글) 표시.
2. 로그인 상태에서 작성 → 입력 즉시 맨 위 반영, 새로고침 후에도 유지(DB 저장 확인).
3. 비로그인 상태에서 작성 시도 → 로그인 모달.

- [ ] **Step 5: 커밋**

```bash
cd /Users/sinseongsu/Desktop/code/ieum
git add src/widgets/shorts-swiper/ui/Shorts.tsx src/widgets/shorts-swiper/ui/CommentDrawer.tsx
git commit -m "feat(comments): CommentDrawer DB 연동(SWR 조회+낙관적 작성)"
```

---

## Self-Review 결과

- **스펙 커버리지:** 엔티티/DTO/조회·작성 API(Task 1~3), 작성자 닉네임+프로필(toDto + 목록 렌더), 인증(POST 가드/`requireAuth`), 최신순·페이지네이션 없음(`orderBy DESC`, 전체 로드), 본문 500자 제한(`@MaxLength`) — 모두 태스크에 매핑됨.
- **플레이스홀더:** 없음(드로어 미변경 마크업은 "기존 유지"로 명시, 코드 블록 제공).
- **타입 일관성:** `CommentDto`(백엔드) ↔ `CommentItem`(프론트) 필드 일치, `getComments`/`createComment` 시그니처가 Task 4 정의와 Task 5 사용처 일치.
- **주의:** `req.user.uid` 필드명은 `likes.controller.ts` 기준 가정 — 실제 필드명이 다르면 그쪽을 따른다(Task 3 Step 2 노트).
